<%= form_with model: @product.comments, url: products_comment_path, method: :post do |pc| %>
    <div>
        <%= pc.hidden_field :product_id, value: @product.id %>
        <%= pc.label :replay, 'Comentário' %>
        <%= pc.text_field :replay %>
    </div>
    <%= pc.submit 'Enviar' %>
<%end%>